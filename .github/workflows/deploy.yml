# Deploys terraform
name: Deploy infrastructure

on:
    workflow_dispatch:

jobs:
  build:
    name: checkout
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v3
